IF (${ROYALE_TARGET_PLATFORM} STREQUAL WINDOWS)
    install(FILES "${CMAKE_CURRENT_SOURCE_DIR}/natvis.md"
            DESTINATION ${ROYALE_INSTALL_DOC_DIR}/natvis)
    install(FILES "${CMAKE_CURRENT_SOURCE_DIR}/royale.natvis"
            DESTINATION ${ROYALE_INSTALL_DOC_DIR}/natvis)
ENDIF()
