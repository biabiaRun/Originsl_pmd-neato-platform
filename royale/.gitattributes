# Always use crlf line endings for inf files. Otherwise the signed cat files
# don't match anymore.
*.inf eol=crlf
# Always use lf line endings for sh files. Otherwise shell will throw mysterious
# errors during script execution.
*.sh  eol=lf

