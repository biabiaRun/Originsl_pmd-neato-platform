INSTALL(DIRECTORY "${CMAKE_CURRENT_SOURCE_DIR}/+royale" DESTINATION "${ROYALE_INSTALL_MATLAB_EXAMPLE_DIR}")
	
INSTALL(FILES "${CMAKE_CURRENT_SOURCE_DIR}/royale_LEVEL1_sample1.m"
    DESTINATION ${ROYALE_INSTALL_MATLAB_EXAMPLE_DIR})        
INSTALL(FILES "${CMAKE_CURRENT_SOURCE_DIR}/royale_LEVEL1_sample2.m"
    DESTINATION ${ROYALE_INSTALL_MATLAB_EXAMPLE_DIR})        
INSTALL(FILES "${CMAKE_CURRENT_SOURCE_DIR}/royale_LEVEL1_sample3.m"
    DESTINATION ${ROYALE_INSTALL_MATLAB_EXAMPLE_DIR})        
INSTALL(FILES "${CMAKE_CURRENT_SOURCE_DIR}/royale_LEVEL1_sample4.m"
    DESTINATION ${ROYALE_INSTALL_MATLAB_EXAMPLE_DIR})        

# Install Level 2 examples only in debug mode

INSTALL(FILES "${CMAKE_CURRENT_SOURCE_DIR}/royale_LEVEL2_sample1.m"
    DESTINATION ${ROYALE_INSTALL_MATLAB_EXAMPLE_DIR} CONFIGURATIONS Debug)        
INSTALL(FILES "${CMAKE_CURRENT_SOURCE_DIR}/royale_LEVEL2_sample2.m"
    DESTINATION ${ROYALE_INSTALL_MATLAB_EXAMPLE_DIR} CONFIGURATIONS Debug)        
INSTALL(FILES "${CMAKE_CURRENT_SOURCE_DIR}/royale_LEVEL2_sample3.m"
    DESTINATION ${ROYALE_INSTALL_MATLAB_EXAMPLE_DIR} CONFIGURATIONS Debug)        
